apiVersion: networking.k8s.io/v1 
kind: Ingress
metadata:
  name: pi
  labels:
    kiamol: ch15
spec:
  rules:
  - host: pi.kiamol.local
    http:
      paths:
      - path: /
        backend:
          service.name: pi-web
          service.port.number: 80